# 2026-09-20 环境准备日志

## 目标

为 mixed-batch 改造建立可复现的单卡性能基线运行环境；本日不执行基准测试。

## 已完成

- 确定最小 mixed-batch 目标及其验收方向。
- 新建 AutoDL 单卡实例：RTX 4090（24,564 MiB）。
- 确认 CUDA Toolkit 位于 `/usr/local/cuda-12.8/bin/nvcc`，并配置 `CUDA_HOME`、`PATH` 与 `OMP_NUM_THREADS`。
- 恢复仓库并记录当前远端基线：`main @ bb823b3e06983d71485a8e1f23715ebd87d98ef8`。
- 创建项目虚拟环境 `.venv`。
- 归档环境快照至 [background.md](background.md)。

## 当前状态

- PyTorch `2.8.0+cu128` 尚未在 `.venv` 中安装完成；此前前台 SSH 连接中断，安装进程已停止。
- 未安装或验证 `transformers`、`flash-attn`、`xxhash`。
- 尚未运行 `example.py`、`bench.py` 或任何性能基线。

## 后续步骤

1. 在 `tmux` 会话中完成 PyTorch 安装，并验证 CUDA 可用。
2. 安装 FlashAttention、Transformers、xxhash 与项目本体。
3. 下载 `Qwen/Qwen3-0.6B`，完成推理 smoke test。
4. 固定工作负载并采集当前实现的性能基线。
